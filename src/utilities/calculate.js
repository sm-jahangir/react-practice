const add = (first, second) => {
    const groundTotal = first + second
    console.log(groundTotal);
}
const multiply = (first, second) => {
    const groundTotal = first * second
    console.log(groundTotal);
}

export {add, multiply};